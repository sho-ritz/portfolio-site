version: "3.8"

services:
  postgres:
    image: postgres:14
    container_name: portfolio_site_backend
    ports:
      - "5437:5432"
    environment:
      POSTGRES_USER: sho
      POSTGRES_PASSWORD: sho
      POSTGRES_DB: portfolio_site_backend
